# Workspace Customizations & Rules

## Git Workflow Rules
- Do NOT automatically execute `git push` commands. Only make local code changes and inform the user, allowing the user to push when ready (or only push when explicitly instructed).
